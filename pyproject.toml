[build-system]
requires = ["setuptools"]
build-backend = "setuptools.build_meta"

[project]
name = "hafox"
version = "0.1.0"

[project.scripts]
hafox = "hafox:main"

[tool.setuptools.packages.find]
where = ["src"]
